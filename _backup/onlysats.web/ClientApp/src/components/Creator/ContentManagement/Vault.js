import React, { Component } from 'react';

export class Vault extends Component {
  static displayName = Vault.name;

  render () {
    return (
      <div>
        <h1>Vault</h1>
        <p>TODO: This is where all content (img, video, audio, and packages of those) will be uploaded and listed</p>
      </div>
    );
  }
}
